# Bun is the only supported package manager for repository dependency updates.
# npm is used only to publish the built tarball; do not create an npm lockfile.
# min-release-age is defense in depth on npm 11.10 and newer only.
package-lock=false
ignore-scripts=true
min-release-age=7
save=false
save-exact=true
