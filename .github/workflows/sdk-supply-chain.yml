name: SDK Rust supply-chain checks

on:
  pull_request:
    paths:
      - ".github/workflows/sdk-supply-chain.yml"
      - "sdk/deny.toml"
      - "sdk/rust/Cargo.toml"
      - "sdk/rust/Cargo.lock"
  push:
    branches: [master]
    paths:
      - ".github/workflows/sdk-supply-chain.yml"
      - "sdk/deny.toml"
      - "sdk/rust/Cargo.toml"
      - "sdk/rust/Cargo.lock"
  schedule:
    - cron: "17 7 * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  sdk-cargo-deny:
    name: SDK RustSec advisories and malicious crates
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout repository
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # was v4
        with:
          persist-credentials: false

      - name: Check SDK RustSec advisories and incident denylist
        uses: EmbarkStudios/cargo-deny-action@3c6349835b2b7b196a839186cb8b78e02f7b5f25 # was v2
        with:
          manifest-path: sdk/rust/Cargo.toml
          command: check advisories bans
          arguments: --config sdk/deny.toml --all-features --locked
