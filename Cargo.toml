[workspace]
resolver = "2"
members = ["crates/maple-agent", "app"]

[workspace.package]
edition = "2021"
version = "0.1.0"
license = "MIT"

[workspace.dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
log = "0.4"
tokio = { version = "1.0", features = ["io-std", "io-util", "net", "process", "sync", "rt-multi-thread", "macros", "time"] }
tokio-util = { version = "0.7", features = ["codec", "compat", "io"] }
anyhow = "1.0"
async-trait = "0.1"
futures-util = "0.3"
once_cell = "1.18.0"
http = "1"
reqwest = { version = "0.13", features = ["stream"] }

[profile.dev]
opt-level = 1

[profile.release]
lto = "thin"
codegen-units = 1
strip = true
